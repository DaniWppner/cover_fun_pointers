From f820617d19478795bac735bbeed5dff5cc5ded20 Mon Sep 17 00:00:00 2001
From: daniel wappner <dwappner@dc.uba.ar>
Date: Wed, 29 Jul 2026 17:56:26 -0300
Subject: [PATCH 08/12] setup InjectFuncPointerTraceAtBlock to depend on
 injectFuncPointerCallbackUsingOffsetChain regardless of trace mode

---
 .../Instrumentation/SanitizerCoverage.cpp     | 135 +++++++++---------
 1 file changed, 66 insertions(+), 69 deletions(-)

diff --git a/llvm/lib/Transforms/Instrumentation/SanitizerCoverage.cpp b/llvm/lib/Transforms/Instrumentation/SanitizerCoverage.cpp
index a6cce5bdd..3a843dba9 100644
--- a/llvm/lib/Transforms/Instrumentation/SanitizerCoverage.cpp
+++ b/llvm/lib/Transforms/Instrumentation/SanitizerCoverage.cpp
@@ -175,6 +175,29 @@ static cl::opt<bool> ClStackDepth("sanitizer-coverage-stack-depth",
 
 namespace {
 
+typedef std::vector<Type *> Types;
+typedef std::vector<unsigned> Offsets;
+// TypesXOffsets represents the flattened path from Type `a` to a Type `b` 
+//  that can be reached through the nested Type structure `a`.
+//  (Type `a` is the last element in the Types array. Type `b` is the first.)
+//  For a given n: int, The last n elements in the Offsets array from back
+//  to front are the indexes used to traverse the nested Type structure `a`
+//  and reach the nth reachable Type in the Type array.
+typedef std::pair<Types, Offsets> TypesXOffsets;
+// vectorTypesXOffsets represents a vector of all the reachable interesting types,
+//  where each reachable interesting type has its corresponding TypesXOffsets entry
+typedef std::vector<TypesXOffsets> vectorTypesXOffsets;
+// retType represents whether a function pointer is reachable from a father type,
+// and the vectorTypesXOffsets to all reachable function pointers.
+typedef std::pair<bool, vectorTypesXOffsets> retType;
+
+struct TraceInstrumentTarget {
+  BasicBlock *BB;
+  StoreInst *SI;
+  Types TypeChain;
+  Offsets OffsetChain;
+};
+
 SanitizerCoverageOptions getOptions(int LegacyCoverageLevel) {
   SanitizerCoverageOptions Res;
   switch (LegacyCoverageLevel) {
@@ -255,8 +278,8 @@ private:
       Function &F, ArrayRef<StoreInst *> InterestingStores);
   void InjectTraceForShallowFunctionPointerStores(
       Function &F, ArrayRef<StoreInst *> InterestingStores);
-  void InjectFuncPointerTraceAtBlock(BasicBlock *BB, StoreInst *SI);
-  void InjectFuncPointerTracesAtBlocks(ArrayRef<std::pair<BasicBlock *, StoreInst *>> BlocksToInstrument,
+  void InjectFuncPointerTraceAtBlock(TraceInstrumentTarget Target);
+  void InjectFuncPointerTracesAtBlocks(ArrayRef<TraceInstrumentTarget> BlocksToInstrument,
                                        json::Object &DebugDump,
                                        json::Array &StoreInstInfoArray,
                                        unsigned InstrumentedCount,
@@ -1006,7 +1029,7 @@ static void ShowPointerVector(std::vector<T *> genericVector) {
 
 // Unused, for debugging purposes
 template <typename T>
-static void ShowElementVector(std::vector<T> genericVector) {
+  void ShowElementVector(std::vector<T> genericVector) {
   errs() << "(size " << genericVector.size() << ") \n{\n";
   for (unsigned i = 0; i < genericVector.size(); i++) {
     errs() << genericVector[i] << "\n";
@@ -1014,30 +1037,7 @@ static void ShowElementVector(std::vector<T> genericVector) {
   errs() << "}\n";
 }
 
-
-typedef std::vector<Type *> Types;
-typedef std::vector<unsigned> Offsets;
-// TypesXOffsets represents the flattened path from Type `a` to a Type  `b`
-//  that can be reached through the nested Type structure `a`.
-//  (Type `a` is the last element in the Types array. Type `b` is the first.)
-//  For a given n: int, The last n elements in the Offsets array from back
-//  to front are the indexes used to traverse the nested Type structure `a`
-//  and reach the nth reachable Type in the Type array.
-typedef std::pair<Types, Offsets> TypesXOffsets;
-// vectorTypesXOffsets represents a vector of all the reachable interesting types,
-//  where each reachable interesting type has its corresponding TypesXOffsets entry
-typedef std::vector<TypesXOffsets> vectorTypesXOffsets;
-// retType represents whether a function pointer is reachable from a father type,
-// and the vectorTypesXOffsets to all reachable function pointers.
-typedef std::pair<bool, vectorTypesXOffsets> retType;
 namespace {
-  struct TraceInstrumentTarget {
-    BasicBlock *BB;
-    StoreInst *SI;
-    Types TypeChain;
-    Offsets OffsetChain;
-  };
-
   // Unused, for debugging purposes
   void showTypeAndOffsetChain(Types TypeChain, Offsets OffsetChain) {
     errs() << "{\n";
@@ -1057,8 +1057,6 @@ namespace {
     errs() << "}\n";
   }
 
-
-
   /*
   Returns all intermediate values that appear while stripping pointer casts.
   */
@@ -1117,7 +1115,6 @@ namespace {
     return Res;
   }
 
-
   retType getOffsetsFromShallowStructType(StructType *STy){
     vectorTypesXOffsets Res;
     for (unsigned j = 0; j < STy->getNumElements(); j++) {
@@ -1172,8 +1169,7 @@ namespace {
     return std::make_pair(false, vectorTypesXOffsets{});
   }
 
-
-  std::pair<Value *, Value *> getFunctionPointerAndAddressUsingOffsetChain(
+  std::pair<Value *, Value *> injectFuncPointerCallbackUsingOffsetChain(
       Value *StorePtr, Value *StoredValue, Offsets offsetChain, Types typeChain,
       IRBuilder<> &Builder) {
 
@@ -1237,7 +1233,6 @@ namespace {
     }
   }
 
-
   bool putFunctionArgFuncTypesIntoJson(Function &F,
                                               json::Object &RootJson) {
     SmallVector<Type *> FuncPointerArgTypes = {};
@@ -1280,26 +1275,28 @@ namespace {
 
 } //namespace
 
-void ModuleSanitizerCoverage::InjectFuncPointerTraceAtBlock(BasicBlock *BB,
-                                                            StoreInst *SI) {
-  Instruction* Terminator = BB->getTerminator();
+void ModuleSanitizerCoverage::InjectFuncPointerTraceAtBlock(TraceInstrumentTarget Target) {
+  Instruction* Terminator = Target.BB->getTerminator();
   DebugLoc EntryLoc = Terminator->getDebugLoc();
   IRBuilder<> IRB(Terminator);
   IRB.SetCurrentDebugLocation(EntryLoc);
-  IRB.CreateCall(SanCovStoreFPointerFunction,
-                 {
-                     IRB.CreatePointerCast(SI->getPointerOperand(), IntptrTy),
-                     IRB.CreatePointerCast(SI->getValueOperand(), IntptrTy),
-                 });
+  injectFuncPointerCallbackUsingOffsetChain(
+    Target.SI->getPointerOperand(),
+    Target.SI->getValueOperand(),
+    Target.OffsetChain,
+    Target.TypeChain,
+    IRB
+  );
 }
-void ModuleSanitizerCoverage::InjectFuncPointerTracesAtBlocks(ArrayRef<std::pair<BasicBlock *, StoreInst *>> BlocksToInstrument,
+
+void ModuleSanitizerCoverage::InjectFuncPointerTracesAtBlocks(ArrayRef<TraceInstrumentTarget> TargetsToInstrument,
   json::Object &DebugDump,
   json::Array &StoreInstInfoArray,
   unsigned InstrumentedCount,
   bool HaveFuncPointerArgTypes
 ){
-  for (const std::pair<BasicBlock *, StoreInst *> &Item : BlocksToInstrument) {
-    InjectFuncPointerTraceAtBlock(Item.first, Item.second);
+  for (TraceInstrumentTarget Item : TargetsToInstrument) {
+    InjectFuncPointerTraceAtBlock(Item);
   }
 
   DebugDump["InstrumentedStores"] = std::move(StoreInstInfoArray);
@@ -1323,27 +1320,33 @@ void ModuleSanitizerCoverage::InjectTraceForNakedFunctionPointerStores(
 
   json::Array StoreInstInfoArray;
   unsigned InstrumentedCount = 0;
-  SmallVector<std::pair<BasicBlock *, StoreInst *>, 8> BlocksToInstrument{};
+  SmallVector<TraceInstrumentTarget, 8> TargetsToInstrument{};
   for (StoreInst *SI : Stores) {
     Value *StoredValue = SI->getValueOperand();
-    if (StoredValue->getType()->isPointerTy())
-      if (StoredValue->getType()->getPointerElementType()->isFunctionTy()) {
+    if (StoredValue->getType()->isPointerTy()){
+      Type* PointeeType = StoredValue->getType()->getPointerElementType();
+      if (PointeeType->isFunctionTy()) {
         InstrumentedCount++;
         BasicBlock *BlockOfSI = SI->getParent();
-        BlocksToInstrument.push_back(std::make_pair(BlockOfSI, SI));
+        TargetsToInstrument.push_back({
+            BlockOfSI,
+            SI,
+            Types{PointeeType},
+            Offsets{0}
+          });
         // Only warn about stores that don't map to source code.
         if (SI->getDebugLoc())
           putStoreInstIntoJson(SI, StoreInstInfoArray);
       }
+    }
   }
-  InjectFuncPointerTracesAtBlocks(BlocksToInstrument,
+  InjectFuncPointerTracesAtBlocks(TargetsToInstrument,
     DebugDump,
     StoreInstInfoArray,
     InstrumentedCount,
     HaveFuncPointerTypeArgs);
 }
 
-
 // On every store to a function pointer variable call we call a run-time
 // function __sanitizer_cov_store_fun_pointer* with two parameters:
 //   - address of the variable,
@@ -1356,37 +1359,31 @@ void ModuleSanitizerCoverage::InjectTraceForShallowFunctionPointerStores(
   bool HaveFuncPointerTypeArgs = putFunctionArgFuncTypesIntoJson(F, DebugDump);
 
   unsigned InstrumentedCount = 0;
-  SmallVector<std::pair<BasicBlock *, StoreInst *>, 8> BlocksToInstrument{};
+  SmallVector<TraceInstrumentTarget, 8> TargetsToInstrument{};
   for (StoreInst *SI : Stores) {
     Value *StoredValue = SI->getValueOperand();
-    Value *StoreTarget = SI->getPointerOperand();
     if (StoredValue->getType()->isPointerTy()){
       SmallVector<Value *, 8> AllIntermediateValues = getAllValuesThatWouldBeStripped(StoredValue);
       retType ShallowOffsets = getOneLevelOffsetsFromAllValues(AllIntermediateValues);
       if (ShallowOffsets.first){
-        for (auto Item : ShallowOffsets.second){
-          auto B = getFunctionPointerAndAddressUsingOffsetChain(StoreTarget,StoredValue,Item.second,Item.first);
+        BasicBlock *BlockOfSI = SI->getParent();
+        for (TypesXOffsets Item : ShallowOffsets.second){
+          TargetsToInstrument.push_back({
+            BlockOfSI,
+            SI,
+            Item.first,
+            Item.second
+          });
+          InstrumentedCount ++;
         }
-      }
-    }
         // Only warn about stores that don't map to source code.
         if (SI->getDebugLoc())
           putStoreInstIntoJson(SI, StoreInstInfoArray);
+      }
+    }
   }
-  // Turn the array into a set of blocks via sort;unique;
-  std::sort(BlocksToInstrument.begin(), BlocksToInstrument.end(),
-            [](std::pair<BasicBlock *, StoreInst *> T1,
-               std::pair<BasicBlock *, StoreInst *> T2) {
-              return T1.first < T2.first;
-            });
-  BlocksToInstrument.erase(
-      std::unique(BlocksToInstrument.begin(), BlocksToInstrument.end(),
-                  [](std::pair<BasicBlock *, StoreInst *> T1,
-                     std::pair<BasicBlock *, StoreInst *> T2) {
-                    return T1.first == T2.first;
-                  }),
-      BlocksToInstrument.end());
-  InjectFuncPointerTracesAtBlocks(BlocksToInstrument,
+
+  InjectFuncPointerTracesAtBlocks(TargetsToInstrument,
     DebugDump,
     StoreInstInfoArray,
     InstrumentedCount,
-- 
2.43.0

